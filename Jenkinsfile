pipeline {
    agent any

    stages {
        stage('Checkout Code') {
            steps {
                echo 'Cloning the repository...'
                // Git clone step
                git url: 'https://github.com/kerenyashar/VS-CODE.git'
            }
        }
    }
}
